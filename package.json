{
  "name": "degree-planner-fullstack",
  "private": true,
  "version": "0.1.0",
  "description": "Scheduler MVP — frontend (Vite/React) + mock Talpix-style backend (FastAPI)",
  "scripts": {
    "dev": "concurrently -n be,fe -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cd backend && python -m uvicorn main:app --reload --port 8000",
    "install:all": "npm --prefix frontend install && pip install -r backend/requirements.txt",
    "seed": "python backend/_seed_from_ts.py"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
